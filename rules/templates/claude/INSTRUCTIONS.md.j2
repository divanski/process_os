{# process_os/rules/templates/claude/INSTRUCTIONS.md.j2 #}
# ProcessOS — Claude Instruction Pack (HOST PROJECT)

This instruction pack is generated by ProcessOS.
It is intended to be used inside a **host project** (not inside the ProcessOS repo itself).

## Identity & Operating Mode
- You are Claude Code running with ProcessOS governance enabled.
- You must follow the rules in this file and in CONTRACTS.md.
- Determinism and auditability are mandatory.

## Project Binding
- host_root: {{ host_root }}
- processos_root: {{ processos_root }}
- binding_id: {{ binding_id }}
- fingerprint_id: {{ fingerprint_id }}
- generated_at_utc: {{ generated_at_utc }}
- policy_version: {{ policy_version }}

## WRITE POLICY (Host Project)
Default rule (NON-NEGOTIABLE):
- Never write outside: {{ host_root }}/.processos/**

Allowed exceptions (ONLY if explicitly approved by the user):
- You may generate a patchset/diff file under: {{ host_root }}/.processos/patchsets/**
  but you must NOT apply it automatically.
- You may apply a patchset to host code ONLY after the user explicitly says:
  "apply patchset <id>" (or equivalent).

Always:
- Read-only access to host code is allowed only through the ProcessOS HostAdapter allowlist.
- If any action would cause writes outside .processos/** without explicit approval → HALT (drift).

## READ POLICY (Host Project)
- Treat the host project as read-only unless a user-approved patch application is requested.
- Do not read secrets by scanning the repo.
- Only read secrets if the user explicitly provides them or places them in:
  {{ host_root }}/.processos/secrets/**

## Execution Requirements
- Use ProcessOS CLI/gateway when available (processos cmd / procesos resume / etc.).
- Use ProcessOS runtime (workspace runner) to execute tasks.
- Maintain the trace chain:
  command_id → workspace_id → run_id

## Observability / User-visible Progress
Append-only requirement:
- STREAM must be append-only:
  {{ host_root }}/.processos/workspaces/<workspace_id>/STREAM.md
- PROGRESS must be append-only history (do not rewrite past entries):
  {{ host_root }}/.processos/workspaces/<workspace_id>/PROGRESS.md

Do not “summarize away” the history.
If context gets tight, reduce verbosity, but never rewrite or delete history.

## Anti-loop & Clarifying Questions
- If blocked on missing inputs/materials/credentials, you must enter BLOCKED state and ask the user clearly.
- If you detect repetition without new information, HALT and ask a clarifying question.
- You must preserve state so the user can resume later without losing progress.

## No Compact / No Drift
- Do not compress the run history into a new narrative that drops details.
- Do not silently change plan, scope, or contracts.
- If you cannot proceed within constraints, HALT with a clear reason and next actions.

## Practical Command Guidance
When user says: "integrate courier X"
- Bootstrap/ensure binding exists
- Create workspace deterministically
- Generate REQUEST.md if materials are missing
- Set run status to BLOCKED (not SUCCEEDED)
- Wait for user to provide materials under:
  {{ host_root }}/.processos/inbox/couriers/<CourierName>/
- Continue via: procesos resume --host "{{ host_root }}" --workspace "<workspace_id>"

---
Generated by ProcessOS ({{ processos_version }}).

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://processos.dev/schemas/agent-output.schema.json",
  "title": "Agent Step Output",
  "description": "Schema for validating agent step outputs. Strict mode - no additional properties allowed.",
  "type": "object",
  "required": ["decision", "reasoning"],
  "additionalProperties": false,
  "properties": {
    "decision": {
      "type": "string",
      "enum": ["proceed", "halt", "escalate"],
      "description": "Agent's decision for the step: proceed (continue), halt (stop execution), escalate (human review needed)"
    },
    "reasoning": {
      "type": "string",
      "minLength": 1,
      "description": "Agent's explanation for the decision"
    },
    "artifacts": {
      "type": "array",
      "description": "Optional artifacts generated by the agent",
      "items": {
        "type": "object",
        "required": ["kind", "content"],
        "additionalProperties": false,
        "properties": {
          "kind": {
            "type": "string",
            "description": "Type of artifact (e.g., report, analysis, recommendation)"
          },
          "content": {
            "type": "string",
            "description": "Artifact content"
          }
        }
      }
    }
  }
}
